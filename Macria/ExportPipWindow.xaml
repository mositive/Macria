<Window x:Class="Macria.ExportPipWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Macria Export"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        Focusable="False" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        FontFamily="{StaticResource AppFont}"
        UseLayoutRounding="True">

    <Border Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="6" Padding="14,10">
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="pipSpinner" Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                       Foreground="{StaticResource LogInfoBrush}"
                       VerticalAlignment="Center"
                       RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <RotateTransform x:Name="pipSpin"/>
                </TextBlock.RenderTransform>
            </TextBlock>
            <TextBlock x:Name="pipStateIcon" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                       Foreground="{StaticResource LogSuccessBrush}"
                       VerticalAlignment="Center" Visibility="Collapsed"/>
            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock FontSize="12" Foreground="{StaticResource TextPrimaryBrush}">
                    <Run Text="Macria" FontWeight="Bold"/>
                    <Run x:Name="pipTitleText" Text=" · DXF Export Sürüyor" FontWeight="SemiBold"/>
                </TextBlock>
                <TextBlock x:Name="pipDetail" Text="" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,2,0,0" MaxWidth="240"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="pipLastLog" Text="" FontSize="10"
                           FontFamily="Cascadia Mono, Consolas"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,3,0,0" MaxWidth="240"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <Button x:Name="btnStop" Click="btnStop_Click" Focusable="False"
                    Margin="16,0,0,0" VerticalAlignment="Center"
                    ToolTip="Acil Durdurma">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#B3342B"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#C9453B"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#93281F"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="bd" Property="Background" Value="#4A2320"/>
                                            <Setter Property="Foreground" Value="#B79E9B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <StackPanel Orientation="Horizontal">
                    <Rectangle Width="9" Height="9" Fill="#FFFFFF" RadiusX="1" RadiusY="1"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="Durdur"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</Window>
